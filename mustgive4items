-- ===== DEBUG: Confirm script started =====
print("SCRIPT: Started loading...")

-- ===== RESET OLD STATE (force fresh start) =====
-- If the script was previously running but GUI crashed, we need to reset
if getgenv().SCRIPT_RUNNING then
    print("SCRIPT: Detected old instance - resetting...")
    -- Destroy any old GUI if it exists
    if gui then
        pcall(function() gui:Destroy() end)
        gui = nil
    end
    -- Reset the running flag so we can start fresh
    getgenv().SCRIPT_RUNNING = false
    print("SCRIPT: Reset complete.")
end

-- Now set running flag
getgenv().SCRIPT_RUNNING = true
print("SCRIPT: Anti-double inject passed.")

-- ===== SETUP =====
print("SCRIPT: Setting up services...")
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local TextChatService = game:GetService("TextChatService")
local TeleportService = game:GetService("TeleportService")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")

print("SCRIPT: Getting local player...")
local player = Players.LocalPlayer
if not player then
    print("SCRIPT: Waiting for LocalPlayer...")
    player = Players:WaitForChild("LocalPlayer")
end
print("SCRIPT: LocalPlayer found: " .. player.Name)

print("SCRIPT: Waiting for Character...")
local character = player.Character or player.CharacterAdded:Wait()
print("SCRIPT: Character ready.")

-- ===== CONFIG =====
local CONFIG = {
    ANNOUNCE_MIN = 19,
    ANNOUNCE_MAX = 24,
    FLING_INTERVAL_MIN = 2,
    FLING_INTERVAL_MAX = 5,
    COUNTDOWN = 5,
    FLING_FORCE = 50000000,
    ANNOUNCEMENTS = true,
    FLING_ENABLED = true,
    RESET_POS = true,
    ANTI_FLING = true,
    AUTO_REJOIN_ON_KICK = true,
}

-- ===== STATE =====
local isActive = false
local firstWarningSent = false
local gui = nil
local announcementConnection = nil
local flingCoroutine = nil

-- ===== BLACKLIST =====
if not getgenv().SERVER_BLACKLIST then
    getgenv().SERVER_BLACKLIST = {}
end
if getgenv().MM2_ACTIVE == nil then
    getgenv().MM2_ACTIVE = false
end
print("SCRIPT: State initialized. MM2_ACTIVE = " .. tostring(getgenv().MM2_ACTIVE))

-- ===== POSITION =====
getgenv().OriginalPos = nil
local function savePosition()
    local root = character and character:FindFirstChild("HumanoidRootPart")
    if root then getgenv().OriginalPos = root.CFrame end
end
local function restorePosition()
    if not CONFIG.RESET_POS or not getgenv().OriginalPos then return end
    local root = character and character:FindFirstChild("HumanoidRootPart")
    if root then
        root.CFrame = getgenv().OriginalPos
        root.Velocity = Vector3.new(0,0,0)
        root.RotVelocity = Vector3.new(0,0,0)
    end
end

-- ===== CHAT =====
local function sendChat(msg)
    if not CONFIG.ANNOUNCEMENTS then return end
    local ok = false
    pcall(function()
        local channel = TextChatService:FindFirstChild("TextChannels")
        if channel then
            channel = channel:FindFirstChild("RBXGeneral") or channel:FindFirstChild("General")
            if channel and channel.SendAsync then
                channel:SendAsync(msg)
                ok = true
                return
            end
        end
    end)
    if not ok then
        pcall(function()
            local chat = ReplicatedStorage:FindFirstChild("DefaultChatSystemChatEvents")
            if chat then
                local req = chat:FindFirstChild("SayMessageRequest")
                if req then req:FireServer(msg, "All") end
            end
        end)
    end
end

-- ===== BLACKLIST FUNCTIONS =====
local function updateBlacklistCounter()
    local count = 0
    for _ in pairs(getgenv().SERVER_BLACKLIST) do count = count + 1 end
    if gui and gui:FindFirstChild("BlacklistLabel") then
        gui.BlacklistLabel.Text = "🚫 " .. count
    end
end
local function addBlacklist()
    local id = game.JobId
    if id and id ~= "" then
        getgenv().SERVER_BLACKLIST[id] = true
        updateBlacklistCounter()
    end
end
local function isBlacklisted()
    return getgenv().SERVER_BLACKLIST[game.JobId] == true
end

-- ===== FLING =====
getgenv().OldPos = nil
getgenv().FPDH = workspace.FallenPartsDestroyHeight

local function flingTarget(target)
    if not CONFIG.FLING_ENABLED then return end
    local char = player.Character
    if not char then return end
    local hum = char:FindFirstChildOfClass("Humanoid")
    if not hum then return end
    local root = hum.RootPart
    if not root then return end
    local tchar = target.Character
    if not tchar then return end
    local thum = tchar:FindFirstChildOfClass("Humanoid")
    if not thum then return end
    local troot = thum.RootPart
    local thead = tchar:FindFirstChild("Head")
    local acc = tchar:FindFirstChildOfClass("Accessory")
    local handle = acc and acc:FindFirstChild("Handle")

    if root.Velocity.Magnitude < 50 then getgenv().OldPos = root.CFrame end
    if thum.Sit then return end

    if thead then workspace.CurrentCamera.CameraSubject = thead
    elseif handle then workspace.CurrentCamera.CameraSubject = handle
    elseif thum and troot then workspace.CurrentCamera.CameraSubject = thum
    end

    if not tchar:FindFirstChildWhichIsA("BasePart") then return end

    local function setPos(part, pos, ang)
        root.CFrame = CFrame.new(part.Position) * pos * ang
        char:SetPrimaryPartCFrame(CFrame.new(part.Position) * pos * ang)
        root.Velocity = Vector3.new(CONFIG.FLING_FORCE, CONFIG.FLING_FORCE * 10, CONFIG.FLING_FORCE)
        root.RotVelocity = Vector3.new(9e8, 9e8, 9e8)
    end

    local function runLoop(basePart)
        local t = tick()
        local angle = 0
        local bv = Instance.new("BodyVelocity")
        bv.Parent = root
        bv.Velocity = Vector3.new(0,0,0)
        bv.MaxForce = Vector3.new(9e9,9e9,9e9)
        hum:SetStateEnabled(Enum.HumanoidStateType.Seated, false)

        repeat
            if root and thum then
                if CONFIG.ANTI_FLING then
                    if not bv.Parent then
                        bv = Instance.new("BodyVelocity")
                        bv.Parent = root
                        bv.Velocity = Vector3.new(0,0,0)
                        bv.MaxForce = Vector3.new(9e9,9e9,9e9)
                    end
                    root.Velocity = Vector3.new(CONFIG.FLING_FORCE, CONFIG.FLING_FORCE * 10, CONFIG.FLING_FORCE)
                    root.RotVelocity = Vector3.new(9e8, 9e8, 9e8)
                end

                if basePart.Velocity.Magnitude < 50 then
                    angle = angle + 100
                    setPos(basePart, CFrame.new(0,1.5,0) + thum.MoveDirection * basePart.Velocity.Magnitude / 1.25, CFrame.Angles(math.rad(angle),0,0))
                    task.wait()
                    setPos(basePart, CFrame.new(0,-1.5,0) + thum.MoveDirection * basePart.Velocity.Magnitude / 1.25, CFrame.Angles(math.rad(angle),0,0))
                    task.wait()
                    setPos(basePart, CFrame.new(0,1.5,0) + thum.MoveDirection * basePart.Velocity.Magnitude / 1.25, CFrame.Angles(math.rad(angle),0,0))
                    task.wait()
                    setPos(basePart, CFrame.new(0,-1.5,0) + thum.MoveDirection * basePart.Velocity.Magnitude / 1.25, CFrame.Angles(math.rad(angle),0,0))
                    task.wait()
                    setPos(basePart, CFrame.new(0,1.5,0) + thum.MoveDirection, CFrame.Angles(math.rad(angle),0,0))
                    task.wait()
                    setPos(basePart, CFrame.new(0,-1.5,0) + thum.MoveDirection, CFrame.Angles(math.rad(angle),0,0))
                    task.wait()
                else
                    setPos(basePart, CFrame.new(0,1.5,thum.WalkSpeed), CFrame.Angles(math.rad(90),0,0))
                    task.wait()
                    setPos(basePart, CFrame.new(0,-1.5,-thum.WalkSpeed), CFrame.Angles(0,0,0))
                    task.wait()
                    setPos(basePart, CFrame.new(0,1.5,thum.WalkSpeed), CFrame.Angles(math.rad(90),0,0))
                    task.wait()
                    setPos(basePart, CFrame.new(0,-1.5,0), CFrame.Angles(math.rad(90),0,0))
                    task.wait()
                    setPos(basePart, CFrame.new(0,-1.5,0), CFrame.Angles(0,0,0))
                    task.wait()
                    setPos(basePart, CFrame.new(0,-1.5,0), CFrame.Angles(math.rad(90),0,0))
                    task.wait()
                    setPos(basePart, CFrame.new(0,-1.5,0), CFrame.Angles(0,0,0))
                    task.wait()
                end
            end
        until t + 2 < tick()
        bv:Destroy()
        hum:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
        workspace.CurrentCamera.CameraSubject = hum
        if getgenv().OldPos then
            repeat
                root.CFrame = getgenv().OldPos * CFrame.new(0,.5,0)
                char:SetPrimaryPartCFrame(getgenv().OldPos * CFrame.new(0,.5,0))
                hum:ChangeState("GettingUp")
                for _, p in pairs(char:GetChildren()) do
                    if p:IsA("BasePart") then
                        p.Velocity, p.RotVelocity = Vector3.new(), Vector3.new()
                    end
                end
                task.wait()
            until (root.Position - getgenv().OldPos.p).Magnitude < 25
        end
        workspace.FallenPartsDestroyHeight = getgenv().FPDH or workspace.FallenPartsDestroyHeight
    end

    workspace.FallenPartsDestroyHeight = 0/0
    if troot then runLoop(troot)
    elseif thead then runLoop(thead)
    elseif handle then runLoop(handle)
    else return end
end

local function flingAll()
    if not isActive or not CONFIG.FLING_ENABLED then return end
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= player then
            pcall(function() flingTarget(p); task.wait(0.1) end)
        end
    end
end

-- ===== LOOPS =====
local function startFlingLoop()
    if flingCoroutine then coroutine.close(flingCoroutine) end
    flingCoroutine = coroutine.create(function()
        while isActive do
            if not firstWarningSent then
                sendChat("Please kindly send a trade my way! 5 seconds remaining till kill all!")
                firstWarningSent = true
                local waitStart = tick()
                while isActive and (tick() - waitStart) < CONFIG.COUNTDOWN do task.wait(0.1) end
                if not isActive then break end
                flingAll()
            else
                flingAll()
            end
            local waitTime = CONFIG.FLING_INTERVAL_MIN + math.random() * (CONFIG.FLING_INTERVAL_MAX - CONFIG.FLING_INTERVAL_MIN)
            local start = tick()
            while isActive and (tick() - start) < waitTime do task.wait(0.5) end
        end
    end)
    coroutine.resume(flingCoroutine)
end

local function startAnnounceLoop()
    if announcementConnection then announcementConnection:Disconnect() end
    if not CONFIG.ANNOUNCEMENTS then return end
    local timer = 0
    announcementConnection = RunService.Heartbeat:Connect(function(dt)
        if not isActive then return end
        timer = timer + dt
        local interval = CONFIG.ANNOUNCE_MIN + math.random() * (CONFIG.ANNOUNCE_MAX - CONFIG.ANNOUNCE_MIN)
        if timer >= interval then
            timer = 0
            sendChat("[ANNOUNCEMENT] i will not stop kill all untill someone gives me 4 items")
        end
    end)
end

-- ===== SYSTEM CONTROLS =====
local function startSystem()
    if isActive then return end
    isActive = true
    getgenv().MM2_ACTIVE = true
    firstWarningSent = false
    savePosition()
    sendChat("[ANNOUNCEMENT] i will not stop kill all untill someone gives me 4 items")
    startAnnounceLoop()
    startFlingLoop()
end

local function stopSystem()
    isActive = false
    getgenv().MM2_ACTIVE = false
    if announcementConnection then announcementConnection:Disconnect(); announcementConnection = nil end
    if flingCoroutine then coroutine.close(flingCoroutine); flingCoroutine = nil end
    restorePosition()
end

local function closeSystem()
    stopSystem()
    getgenv().SCRIPT_RUNNING = false
    if gui then gui:Destroy(); gui = nil end
end

local function serverHop()
    addBlacklist()
    pcall(function() TeleportService:Teleport(game.PlaceId) end)
end

-- ===== AUTO-REJOIN ON KICK =====
if CONFIG.AUTO_REJOIN_ON_KICK then
    game:OnTeleport(function(state)
        if state == Enum.TeleportState.Failed then
            task.wait(2)
            serverHop()
        end
    end)
end

-- ===== GUI CREATION =====
print("SCRIPT: Starting GUI creation...")
local function createGUI()
    -- Destroy any existing GUI
    if gui then
        pcall(function() gui:Destroy() end)
        gui = nil
    end

    print("SCRIPT: Creating new ScreenGui...")
    gui = Instance.new("ScreenGui")
    gui.Name = "MM2FlingGUI"
    gui.ResetOnSpawn = false

    -- Try PlayerGui first, then CoreGui
    local parentSuccess = false
    pcall(function()
        gui.Parent = player:WaitForChild("PlayerGui")
        parentSuccess = true
    end)
    if not parentSuccess then
        pcall(function()
            gui.Parent = game:GetService("CoreGui")
            parentSuccess = true
        end)
    end
    if not parentSuccess then
        warn("SCRIPT: Could not parent GUI – aborting.")
        return nil
    end
    print("SCRIPT: GUI parented successfully.")

    -- Main window (SIMPLE – guaranteed to show)
    local window = Instance.new("Frame")
    window.Size = UDim2.new(0, 500, 0, 420)
    window.Position = UDim2.new(0.5, -250, 0.5, -210)
    window.BackgroundColor3 = Color3.fromRGB(10,10,13)
    window.BackgroundTransparency = 0.05
    window.BorderSizePixel = 0
    window.ClipsDescendants = true
    window.Active = true
    window.Draggable = true
    window.Parent = gui

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 12)
    corner.Parent = window

    -- TOP BAR
    local topBar = Instance.new("Frame")
    topBar.Size = UDim2.new(1,0,0,32)
    topBar.BackgroundColor3 = Color3.fromRGB(10,10,13)
    topBar.BackgroundTransparency = 0.3
    topBar.Parent = window

    -- Title
    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(0.6,0,1,0)
    title.Position = UDim2.new(0,12,0,0)
    title.BackgroundTransparency = 1
    title.Text = "fling v2"
    title.TextColor3 = Color3.fromRGB(214,133,200)
    title.TextScaled = true
    title.Font = Enum.Font.GothamBold
    title.TextXAlignment = Enum.TextXAlignment.Left
    title.Parent = topBar

    -- Close button
    local closeBtn = Instance.new("TextButton")
    closeBtn.Size = UDim2.new(0,24,0,24)
    closeBtn.Position = UDim2.new(1,-30,0.5,-12)
    closeBtn.Text = "✕"
    closeBtn.TextColor3 = Color3.fromRGB(216,216,216)
    closeBtn.BackgroundTransparency = 1
    closeBtn.TextScaled = true
    closeBtn.Font = Enum.Font.GothamBold
    closeBtn.BorderSizePixel = 0
    closeBtn.Parent = topBar
    closeBtn.MouseButton1Click:Connect(closeSystem)

    -- CONTENT
    local content = Instance.new("Frame")
    content.Size = UDim2.new(1,-20,1,-50)
    content.Position = UDim2.new(0,10,0,42)
    content.BackgroundTransparency = 1
    content.Parent = window

    -- Row 1: Status + Buttons
    local row1 = Instance.new("Frame")
    row1.Size = UDim2.new(1,0,0,36)
    row1.BackgroundTransparency = 1
    row1.Parent = content

    -- Status dot
    local dot = Instance.new("Frame")
    dot.Size = UDim2.new(0,10,0,10)
    dot.Position = UDim2.new(0,0,0.5,-5)
    dot.BackgroundColor3 = Color3.fromRGB(255,80,80)
    dot.BorderSizePixel = 0
    dot.Parent = row1
    local dotCorner = Instance.new("UICorner")
    dotCorner.CornerRadius = UDim.new(1,0)
    dotCorner.Parent = dot

    -- Status text
    local statusText = Instance.new("TextLabel")
    statusText.Size = UDim2.new(0.3,0,1,0)
    statusText.Position = UDim2.new(0,18,0,0)
    statusText.BackgroundTransparency = 1
    statusText.Text = "OFFLINE"
    statusText.TextColor3 = Color3.fromRGB(180,180,190)
    statusText.TextScaled = true
    statusText.Font = Enum.Font.GothamBold
    statusText.TextXAlignment = Enum.TextXAlignment.Left
    statusText.Name = "StatusText"
    statusText.Parent = row1

    -- START/STOP button
    local toggleBtn = Instance.new("TextButton")
    toggleBtn.Size = UDim2.new(0,80,1,0)
    toggleBtn.Position = UDim2.new(0.4,0,0,0)
    toggleBtn.Text = "START"
    toggleBtn.TextColor3 = Color3.fromRGB(255,255,255)
    toggleBtn.BackgroundColor3 = Color3.fromRGB(199,127,192)
    toggleBtn.BackgroundTransparency = 0.2
    toggleBtn.TextScaled = true
    toggleBtn.Font = Enum.Font.GothamBold
    toggleBtn.BorderSizePixel = 0
    toggleBtn.Parent = row1
    local tCorner = Instance.new("UICorner")
    tCorner.CornerRadius = UDim.new(0,8)
    tCorner.Parent = toggleBtn

    -- S-HOP button
    local sHopBtn = Instance.new("TextButton")
    sHopBtn.Size = UDim2.new(0,60,1,0)
    sHopBtn.Position = UDim2.new(0.65,0,0,0)
    sHopBtn.Text = "S-HOP"
    sHopBtn.TextColor3 = Color3.fromRGB(255,255,255)
    sHopBtn.BackgroundColor3 = Color3.fromRGB(50,50,80)
    sHopBtn.BackgroundTransparency = 0.3
    sHopBtn.TextScaled = true
    sHopBtn.Font = Enum.Font.GothamBold
    sHopBtn.BorderSizePixel = 0
    sHopBtn.Parent = row1
    local hCorner = Instance.new("UICorner")
    hCorner.CornerRadius = UDim.new(0,8)
    hCorner.Parent = sHopBtn
    sHopBtn.MouseButton1Click:Connect(serverHop)

    -- Blacklist counter
    local blacklistLabel = Instance.new("TextLabel")
    blacklistLabel.Size = UDim2.new(0.15,0,1,0)
    blacklistLabel.Position = UDim2.new(0.8,0,0,0)
    blacklistLabel.BackgroundTransparency = 1
    blacklistLabel.Text = "🚫 0"
    blacklistLabel.TextColor3 = Color3.fromRGB(180,180,190)
    blacklistLabel.TextScaled = true
    blacklistLabel.Font = Enum.Font.Gotham
    blacklistLabel.TextXAlignment = Enum.TextXAlignment.Right
    blacklistLabel.Name = "BlacklistLabel"
    blacklistLabel.Parent = row1
    gui.BlacklistLabel = blacklistLabel

    -- Row 2: Info labels
    local row2 = Instance.new("Frame")
    row2.Size = UDim2.new(1,0,0,32)
    row2.Position = UDim2.new(0,0,0,46)
    row2.BackgroundTransparency = 1
    row2.Parent = content

    local announceLabel = Instance.new("TextLabel")
    announceLabel.Size = UDim2.new(0.45,0,1,0)
    announceLabel.BackgroundTransparency = 1
    announceLabel.Text = "Announce: " .. CONFIG.ANNOUNCE_MIN .. "-" .. CONFIG.ANNOUNCE_MAX .. "s"
    announceLabel.TextColor3 = Color3.fromRGB(160,160,170)
    announceLabel.TextScaled = true
    announceLabel.Font = Enum.Font.Gotham
    announceLabel.TextXAlignment = Enum.TextXAlignment.Left
    announceLabel.Parent = row2

    local flingIntervalLabel = Instance.new("TextLabel")
    flingIntervalLabel.Size = UDim2.new(0.45,0,1,0)
    flingIntervalLabel.Position = UDim2.new(0.5,0,0,0)
    flingIntervalLabel.BackgroundTransparency = 1
    flingIntervalLabel.Text = "Fling: " .. CONFIG.FLING_INTERVAL_MIN .. "-" .. CONFIG.FLING_INTERVAL_MAX .. "s"
    flingIntervalLabel.TextColor3 = Color3.fromRGB(160,160,170)
    flingIntervalLabel.TextScaled = true
    flingIntervalLabel.Font = Enum.Font.Gotham
    flingIntervalLabel.TextXAlignment = Enum.TextXAlignment.Left
    flingIntervalLabel.Parent = row2

    -- Row 3: Toggles (4 toggles in 2x2 grid)
    local toggles = {
        {label = "Announcements", var = "ANNOUNCEMENTS", default = true},
        {label = "Fling", var = "FLING_ENABLED", default = true},
        {label = "Reset Pos", var = "RESET_POS", default = true},
        {label = "Anti-Fling", var = "ANTI_FLING", default = true},
    }
    for i, t in ipairs(toggles) do
        local row = Instance.new("Frame")
        row.Size = UDim2.new(0.5, 0, 0, 28)
        row.Position = UDim2.new((i-1)%2 * 0.5, 0, 0, 86 + math.floor((i-1)/2) * 32)
        row.BackgroundTransparency = 1
        row.Parent = content

        local lbl = Instance.new("TextLabel")
        lbl.Size = UDim2.new(0.6, 0, 1, 0)
        lbl.BackgroundTransparency = 1
        lbl.Text = t.label
        lbl.TextColor3 = Color3.fromRGB(180,180,190)
        lbl.TextScaled = true
        lbl.Font = Enum.Font.Gotham
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.Parent = row

        local toggle = Instance.new("TextButton")
        toggle.Size = UDim2.new(0,22,0,20)
        toggle.Position = UDim2.new(0.7,0,0.5,-10)
        toggle.Text = ""
        toggle.BackgroundColor3 = Color3.fromRGB(30,30,40)
        toggle.BackgroundTransparency = 0.2
        toggle.BorderSizePixel = 0
        toggle.Parent = row
        local tCorner2 = Instance.new("UICorner")
        tCorner2.CornerRadius = UDim.new(0,5)
        tCorner2.Parent = toggle

        local fill = Instance.new("Frame")
        fill.Size = UDim2.new(1,0,1,0)
        fill.BackgroundColor3 = Color3.fromRGB(199,127,192)
        fill.BackgroundTransparency = t.default and 0.1 or 1
        fill.BorderSizePixel = 0
        fill.Parent = toggle
        local fCorner = Instance.new("UICorner")
        fCorner.CornerRadius = UDim.new(0,5)
        fCorner.Parent = fill

        local isOn = t.default
        toggle.MouseButton1Click:Connect(function()
            isOn = not isOn
            fill.BackgroundTransparency = isOn and 0.1 or 1
            CONFIG[t.var] = isOn
            if t.var == "ANNOUNCEMENTS" then
                if isActive then
                    if isOn then startAnnounceLoop() else
                        if announcementConnection then announcementConnection:Disconnect(); announcementConnection = nil end
                    end
                end
            end
        end)
    end

    -- Row 4: Fling Force Slider
    local row4 = Instance.new("Frame")
    row4.Size = UDim2.new(1,0,0,30)
    row4.Position = UDim2.new(0,0,0,160)
    row4.BackgroundTransparency = 1
    row4.Parent = content

    local forceLabel = Instance.new("TextLabel")
    forceLabel.Size = UDim2.new(0.3,0,1,0)
    forceLabel.BackgroundTransparency = 1
    forceLabel.Text = "Force"
    forceLabel.TextColor3 = Color3.fromRGB(180,180,190)
    forceLabel.TextScaled = true
    forceLabel.Font = Enum.Font.Gotham
    forceLabel.TextXAlignment = Enum.TextXAlignment.Left
    forceLabel.Parent = row4

    local sliderTrack = Instance.new("TextButton")
    sliderTrack.Size = UDim2.new(0.5,0,0.6,0)
    sliderTrack.Position = UDim2.new(0.3,0,0.5,-8)
    sliderTrack.BackgroundColor3 = Color3.fromRGB(28,28,37)
    sliderTrack.BorderSizePixel = 0
    sliderTrack.Parent = row4
    local stCorner = Instance.new("UICorner")
    stCorner.CornerRadius = UDim.new(0,999)
    stCorner.Parent = sliderTrack

    local sliderFill = Instance.new("Frame")
    sliderFill.Size = UDim2.new(0.5,0,1,0)
    sliderFill.BackgroundColor3 = Color3.fromRGB(181,111,175)
    sliderFill.BorderSizePixel = 0
    sliderFill.Parent = sliderTrack
    local sfCorner = Instance.new("UICorner")
    sfCorner.CornerRadius = UDim.new(0,999)
    sfCorner.Parent = sliderFill

    local forceValue = Instance.new("TextLabel")
    forceValue.Size = UDim2.new(0.15,0,1,0)
    forceValue.Position = UDim2.new(0.82,0,0,0)
    forceValue.BackgroundTransparency = 1
    forceValue.Text = "5e7"
    forceValue.TextColor3 = Color3.fromRGB(180,180,190)
    forceValue.TextScaled = true
    forceValue.Font = Enum.Font.Gotham
    forceValue.TextXAlignment = Enum.TextXAlignment.Left
    forceValue.Name = "ForceValue"
    forceValue.Parent = row4

    -- Slider drag logic
    local dragging = false
    local function updateSliderFromMouse(x)
        local trackAbsPos = sliderTrack.AbsolutePosition
        local trackSize = sliderTrack.AbsoluteSize
        local relX = math.clamp((x - trackAbsPos.X) / trackSize.X, 0, 1)
        sliderFill.Size = UDim2.new(relX, 0, 1, 0)
        local force = relX * 90000000
        if force < 1000000 then force = 1000000 end
        CONFIG.FLING_FORCE = force
        forceValue.Text = string.format("%.1e", force)
    end

    sliderTrack.MouseButton1Down:Connect(function(x, y)
        dragging = true
        updateSliderFromMouse(x)
    end)

    UserInputService.InputChanged:Connect(function(input, processed)
        if processed then return end
        if input.UserInputType == Enum.UserInputType.MouseMovement and dragging then
            updateSliderFromMouse(input.Position.X)
        end
    end)

    UserInputService.InputEnded:Connect(function(input, processed)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = false
        end
    end)

    -- Row 5: Auto-Rejoin on Kick toggle
    local row5 = Instance.new("Frame")
    row5.Size = UDim2.new(1,0,0,28)
    row5.Position = UDim2.new(0,0,0,195)
    row5.BackgroundTransparency = 1
    row5.Parent = content

    local kickLabel = Instance.new("TextLabel")
    kickLabel.Size = UDim2.new(0.6,0,1,0)
    kickLabel.BackgroundTransparency = 1
    kickLabel.Text = "Auto Rejoin on Kick"
    kickLabel.TextColor3 = Color3.fromRGB(180,180,190)
    kickLabel.TextScaled = true
    kickLabel.Font = Enum.Font.Gotham
    kickLabel.TextXAlignment = Enum.TextXAlignment.Left
    kickLabel.Parent = row5

    local kickToggle = Instance.new("TextButton")
    kickToggle.Size = UDim2.new(0,22,0,20)
    kickToggle.Position = UDim2.new(0.7,0,0.5,-10)
    kickToggle.Text = ""
    kickToggle.BackgroundColor3 = Color3.fromRGB(30,30,40)
    kickToggle.BackgroundTransparency = 0.2
    kickToggle.BorderSizePixel = 0
    kickToggle.Parent = row5
    local ktCorner = Instance.new("UICorner")
    ktCorner.CornerRadius = UDim.new(0,5)
    ktCorner.Parent = kickToggle

    local kickFill = Instance.new("Frame")
    kickFill.Size = UDim2.new(1,0,1,0)
    kickFill.BackgroundColor3 = Color3.fromRGB(199,127,192)
    kickFill.BackgroundTransparency = CONFIG.AUTO_REJOIN_ON_KICK and 0.1 or 1
    kickFill.BorderSizePixel = 0
    kickFill.Parent = kickToggle
    local kfCorner = Instance.new("UICorner")
    kfCorner.CornerRadius = UDim.new(0,5)
    kfCorner.Parent = kickFill

    local kickOn = CONFIG.AUTO_REJOIN_ON_KICK
    kickToggle.MouseButton1Click:Connect(function()
        kickOn = not kickOn
        kickFill.BackgroundTransparency = kickOn and 0.1 or 1
        CONFIG.AUTO_REJOIN_ON_KICK = kickOn
    end)

    -- Toggle button logic
    toggleBtn.MouseButton1Click:Connect(function()
        if isActive then
            stopSystem()
            toggleBtn.Text = "START"
            toggleBtn.BackgroundColor3 = Color3.fromRGB(199,127,192)
            dot.BackgroundColor3 = Color3.fromRGB(255,80,80)
            statusText.Text = "OFFLINE"
            statusText.TextColor3 = Color3.fromRGB(180,180,190)
        else
            startSystem()
            toggleBtn.Text = "STOP"
            toggleBtn.BackgroundColor3 = Color3.fromRGB(200,60,60)
            dot.BackgroundColor3 = Color3.fromRGB(100,255,100)
            statusText.Text = "ONLINE"
            statusText.TextColor3 = Color3.fromRGB(100,255,100)
        end
        updateBlacklistCounter()
    end)

    -- Auto-start if previously active
    if getgenv().MM2_ACTIVE then
        startSystem()
        toggleBtn.Text = "STOP"
        toggleBtn.BackgroundColor3 = Color3.fromRGB(200,60,60)
        dot.BackgroundColor3 = Color3.fromRGB(100,255,100)
        statusText.Text = "ONLINE"
        statusText.TextColor3 = Color3.fromRGB(100,255,100)
        updateBlacklistCounter()
    end

    updateBlacklistCounter()
    print("SCRIPT: GUI fully created.")
    return gui
end

-- ===== CREATE THE GUI =====
print("SCRIPT: Calling createGUI()...")
local success, err = pcall(createGUI)
if success then
    print("SCRIPT: GUI created successfully.")
else
    warn("SCRIPT: Failed to create GUI! Error: " .. tostring(err))
end

-- ===== PERSISTENCE =====
pcall(function()
    if queue_on_teleport then
        queue_on_teleport([[
            loadstring(game:HttpGet("https://raw.githubusercontent.com/ddawwn/mm2/refs/heads/main/mustgive4items"))()
        ]])
    end
end)

-- ===== BLACKLIST CHECK =====
local function checkBlacklistAndRejoin()
    if isBlacklisted() then
        print("Blacklisted server detected – hopping again.")
        task.wait(1)
        pcall(function() TeleportService:Teleport(game.PlaceId) end)
    end
end
spawn(function()
    task.wait(1)
    checkBlacklistAndRejoin()
end)

print("SCRIPT: Full load complete.")
