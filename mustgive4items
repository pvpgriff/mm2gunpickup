-- ============================================================
--    TRADECOLLECTOR v2.1 – Fixed Drag & Slider
--    Only title bar draggable; slider works correctly.
-- ============================================================

-- Anti‑duplicate injection
if getgenv().SCRIPT_RUNNING then return end
getgenv().SCRIPT_RUNNING = true
getgenv().KEEP_ALIVE = true

if not getgenv().TELEPORT_QUEUED then
    getgenv().TELEPORT_QUEUED = true
    pcall(function()
        if queue_on_teleport then
            queue_on_teleport([[
                if getgenv().KEEP_ALIVE then
                    loadstring(game:HttpGet("https://raw.githubusercontent.com/ddawwn/mm2/refs/heads/main/mustgive4items"))()
                end
            ]])
        end
    end)
end

-- ============================================================
-- SERVICES
-- ============================================================
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local TextChatService = game:GetService("TextChatService")
local TeleportService = game:GetService("TeleportService")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")

local player = Players.LocalPlayer
if not player then player = Players:WaitForChild("LocalPlayer") end
local character = player.Character or player.CharacterAdded:Wait()

-- ============================================================
-- CONFIG & STATE
-- ============================================================
local CONFIG = {
    ANNOUNCE_MIN = 19, ANNOUNCE_MAX = 24,
    FLING_INTERVAL_MIN = 2, FLING_INTERVAL_MAX = 5,
    COUNTDOWN = 5,
    FLING_FORCE = 50000000,
    ANNOUNCEMENTS = true,
    FLING_ENABLED = true,
    RESET_POS = true,
    ANTI_FLING = true,
}

local isActive = false
local firstWarningSent = false
local gui = nil
local announcementConnection = nil
local flingCoroutine = nil
local blacklistLabelRef = nil

if not getgenv().SERVER_BLACKLIST then getgenv().SERVER_BLACKLIST = {} end
if getgenv().MM2_ACTIVE == nil then getgenv().MM2_ACTIVE = false end
getgenv().OriginalPos = nil

-- ============================================================
-- CORE FUNCTIONS (unchanged)
-- ============================================================
local function savePosition()
    local root = character and character:FindFirstChild("HumanoidRootPart")
    if root then getgenv().OriginalPos = root.CFrame end
end

local function restorePosition()
    if not CONFIG.RESET_POS or not getgenv().OriginalPos then return end
    local root = character and character:FindFirstChild("HumanoidRootPart")
    if root then
        root.CFrame = getgenv().OriginalPos
        root.Velocity = Vector3.new(0,0,0)
        root.RotVelocity = Vector3.new(0,0,0)
    end
end

local function sendChat(msg)
    if not CONFIG.ANNOUNCEMENTS then return end
    local ok = false
    pcall(function()
        local channel = TextChatService:FindFirstChild("TextChannels")
        if channel then
            channel = channel:FindFirstChild("RBXGeneral") or channel:FindFirstChild("General")
            if channel and channel.SendAsync then
                channel:SendAsync(msg)
                ok = true
                return
            end
        end
    end)
    if not ok then
        pcall(function()
            local chat = ReplicatedStorage:FindFirstChild("DefaultChatSystemChatEvents")
            if chat then
                local req = chat:FindFirstChild("SayMessageRequest")
                if req then req:FireServer(msg, "All") end
            end
        end)
    end
end

local function updateBlacklistCounter()
    local count = 0
    for _ in pairs(getgenv().SERVER_BLACKLIST) do count = count + 1 end
    if blacklistLabelRef then
        blacklistLabelRef.Text = "🚫 " .. count
    end
end

local function addBlacklist()
    local id = game.JobId
    if id and id ~= "" then
        getgenv().SERVER_BLACKLIST[id] = true
        updateBlacklistCounter()
    end
end

local function isBlacklisted()
    return getgenv().SERVER_BLACKLIST[game.JobId] == true
end

local function hasFriendsInServer()
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= player and p:IsFriendsWith(player.UserId) then
            return true
        end
    end
    return false
end

-- ============================================================
-- FLING ENGINE (unchanged)
-- ============================================================
getgenv().OldPos = nil
getgenv().FPDH = workspace.FallenPartsDestroyHeight

local function flingTarget(target)
    if not CONFIG.FLING_ENABLED then return end
    local char = player.Character
    if not char then return end
    local hum = char:FindFirstChildOfClass("Humanoid")
    if not hum then return end
    local root = hum.RootPart
    if not root then return end
    local tchar = target.Character
    if not tchar then return end
    local thum = tchar:FindFirstChildOfClass("Humanoid")
    if not thum then return end
    local troot = thum.RootPart
    local thead = tchar:FindFirstChild("Head")
    local acc = tchar:FindFirstChildOfClass("Accessory")
    local handle = acc and acc:FindFirstChild("Handle")

    if root.Velocity.Magnitude < 50 then getgenv().OldPos = root.CFrame end
    if thum.Sit then return end

    if thead then workspace.CurrentCamera.CameraSubject = thead
    elseif handle then workspace.CurrentCamera.CameraSubject = handle
    elseif thum and troot then workspace.CurrentCamera.CameraSubject = thum
    end

    if not tchar:FindFirstChildWhichIsA("BasePart") then return end

    local function setPos(part, pos, ang)
        root.CFrame = CFrame.new(part.Position) * pos * ang
        char:SetPrimaryPartCFrame(CFrame.new(part.Position) * pos * ang)
        root.Velocity = Vector3.new(CONFIG.FLING_FORCE, CONFIG.FLING_FORCE * 10, CONFIG.FLING_FORCE)
        root.RotVelocity = Vector3.new(9e8, 9e8, 9e8)
    end

    local function runLoop(basePart)
        local t = tick()
        local angle = 0
        local bv = Instance.new("BodyVelocity")
        bv.Parent = root
        bv.Velocity = Vector3.new(0,0,0)
        bv.MaxForce = Vector3.new(9e9,9e9,9e9)
        hum:SetStateEnabled(Enum.HumanoidStateType.Seated, false)

        repeat
            if root and thum then
                if CONFIG.ANTI_FLING then
                    if not bv.Parent then
                        bv = Instance.new("BodyVelocity")
                        bv.Parent = root
                        bv.Velocity = Vector3.new(0,0,0)
                        bv.MaxForce = Vector3.new(9e9,9e9,9e9)
                    end
                    root.Velocity = Vector3.new(CONFIG.FLING_FORCE, CONFIG.FLING_FORCE * 10, CONFIG.FLING_FORCE)
                    root.RotVelocity = Vector3.new(9e8, 9e8, 9e8)
                end

                if basePart.Velocity.Magnitude < 50 then
                    angle = angle + 100
                    setPos(basePart, CFrame.new(0,1.5,0) + thum.MoveDirection * basePart.Velocity.Magnitude / 1.25, CFrame.Angles(math.rad(angle),0,0))
                    task.wait()
                    setPos(basePart, CFrame.new(0,-1.5,0) + thum.MoveDirection * basePart.Velocity.Magnitude / 1.25, CFrame.Angles(math.rad(angle),0,0))
                    task.wait()
                    setPos(basePart, CFrame.new(0,1.5,0) + thum.MoveDirection * basePart.Velocity.Magnitude / 1.25, CFrame.Angles(math.rad(angle),0,0))
                    task.wait()
                    setPos(basePart, CFrame.new(0,-1.5,0) + thum.MoveDirection * basePart.Velocity.Magnitude / 1.25, CFrame.Angles(math.rad(angle),0,0))
                    task.wait()
                    setPos(basePart, CFrame.new(0,1.5,0) + thum.MoveDirection, CFrame.Angles(math.rad(angle),0,0))
                    task.wait()
                    setPos(basePart, CFrame.new(0,-1.5,0) + thum.MoveDirection, CFrame.Angles(math.rad(angle),0,0))
                    task.wait()
                else
                    setPos(basePart, CFrame.new(0,1.5,thum.WalkSpeed), CFrame.Angles(math.rad(90),0,0))
                    task.wait()
                    setPos(basePart, CFrame.new(0,-1.5,-thum.WalkSpeed), CFrame.Angles(0,0,0))
                    task.wait()
                    setPos(basePart, CFrame.new(0,1.5,thum.WalkSpeed), CFrame.Angles(math.rad(90),0,0))
                    task.wait()
                    setPos(basePart, CFrame.new(0,-1.5,0), CFrame.Angles(math.rad(90),0,0))
                    task.wait()
                    setPos(basePart, CFrame.new(0,-1.5,0), CFrame.Angles(0,0,0))
                    task.wait()
                    setPos(basePart, CFrame.new(0,-1.5,0), CFrame.Angles(math.rad(90),0,0))
                    task.wait()
                    setPos(basePart, CFrame.new(0,-1.5,0), CFrame.Angles(0,0,0))
                    task.wait()
                end
            end
        until t + 2 < tick()
        bv:Destroy()
        hum:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
        workspace.CurrentCamera.CameraSubject = hum
        if getgenv().OldPos then
            repeat
                root.CFrame = getgenv().OldPos * CFrame.new(0,.5,0)
                char:SetPrimaryPartCFrame(getgenv().OldPos * CFrame.new(0,.5,0))
                hum:ChangeState("GettingUp")
                for _, p in pairs(char:GetChildren()) do
                    if p:IsA("BasePart") then
                        p.Velocity, p.RotVelocity = Vector3.new(), Vector3.new()
                    end
                end
                task.wait()
            until (root.Position - getgenv().OldPos.p).Magnitude < 25
        end
        workspace.FallenPartsDestroyHeight = getgenv().FPDH or workspace.FallenPartsDestroyHeight
    end

    workspace.FallenPartsDestroyHeight = 0/0
    if troot then runLoop(troot)
    elseif thead then runLoop(thead)
    elseif handle then runLoop(handle)
    else return end
end

local function flingAll()
    if not isActive or not CONFIG.FLING_ENABLED then return end
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= player then
            pcall(function() flingTarget(p); task.wait(0.1) end)
        end
    end
end

-- ============================================================
-- LOOPS
-- ============================================================
local function startFlingLoop()
    if flingCoroutine then coroutine.close(flingCoroutine) end
    flingCoroutine = coroutine.create(function()
        while isActive do
            if not firstWarningSent then
                sendChat("Please kindly send a trade my way! 5 seconds remaining till kill all!")
                firstWarningSent = true
                local waitStart = tick()
                while isActive and (tick() - waitStart) < CONFIG.COUNTDOWN do task.wait(0.1) end
                if not isActive then break end
                flingAll()
            else
                flingAll()
            end
            local waitTime = CONFIG.FLING_INTERVAL_MIN + math.random() * (CONFIG.FLING_INTERVAL_MAX - CONFIG.FLING_INTERVAL_MIN)
            local start = tick()
            while isActive and (tick() - start) < waitTime do task.wait(0.5) end
        end
    end)
    coroutine.resume(flingCoroutine)
end

local function startAnnounceLoop()
    if announcementConnection then announcementConnection:Disconnect() end
    if not CONFIG.ANNOUNCEMENTS then return end
    local timer = 0
    announcementConnection = RunService.Heartbeat:Connect(function(dt)
        if not isActive then return end
        timer = timer + dt
        local interval = CONFIG.ANNOUNCE_MIN + math.random() * (CONFIG.ANNOUNCE_MAX - CONFIG.ANNOUNCE_MIN)
        if timer >= interval then
            timer = 0
            sendChat("[ANNOUNCEMENT] i will not stop kill all untill someone gives me 4 items")
        end
    end)
end

-- ============================================================
-- SYSTEM CONTROLS
-- ============================================================
local function startSystem()
    if isActive then return end
    isActive = true
    getgenv().MM2_ACTIVE = true
    firstWarningSent = false
    savePosition()
    sendChat("[ANNOUNCEMENT] i will not stop kill all untill someone gives me 4 items")
    startAnnounceLoop()
    startFlingLoop()
end

local function stopSystem()
    isActive = false
    getgenv().MM2_ACTIVE = false
    if announcementConnection then announcementConnection:Disconnect(); announcementConnection = nil end
    if flingCoroutine then coroutine.close(flingCoroutine); flingCoroutine = nil end
    restorePosition()
end

local function closeSystem()
    stopSystem()
    getgenv().SCRIPT_RUNNING = false
    getgenv().KEEP_ALIVE = false
    if gui then gui:Destroy(); gui = nil end
end

local function serverHop()
    addBlacklist()
    pcall(function() TeleportService:Teleport(game.PlaceId) end)
end

local function checkBlacklistAndFriends()
    if isBlacklisted() then
        task.wait(1)
        pcall(function() TeleportService:Teleport(game.PlaceId) end)
        return
    end
    if hasFriendsInServer() then
        addBlacklist()
        task.wait(1)
        pcall(function() TeleportService:Teleport(game.PlaceId) end)
    end
end

-- ============================================================
-- PREMIUM UI WITH FIXED DRAG & SLIDER
-- ============================================================
local function createPremiumGUI()
    if gui then pcall(function() gui:Destroy() end); gui = nil end

    gui = Instance.new("ScreenGui")
    gui.Name = "TradeCollectorGUI"
    gui.ResetOnSpawn = false
    gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    gui.DisplayOrder = 999

    local parentSuccess = false
    pcall(function()
        gui.Parent = player:WaitForChild("PlayerGui")
        parentSuccess = true
    end)
    if not parentSuccess then
        pcall(function()
            gui.Parent = game:GetService("CoreGui")
            parentSuccess = true
        end)
    end
    if not parentSuccess then return nil end

    -- MAIN WINDOW – not draggable itself
    local window = Instance.new("Frame")
    window.Size = UDim2.new(0, 820, 0, 460)
    window.Position = UDim2.new(0.5, -410, 0.5, -230)
    window.BackgroundColor3 = Color3.fromRGB(10, 10, 13)
    window.BackgroundTransparency = 0
    window.BorderSizePixel = 0
    window.ClipsDescendants = true
    window.Active = false          -- disable built-in dragging
    window.Draggable = false
    window.Parent = gui

    -- Shadow
    local shadow = Instance.new("Frame")
    shadow.Size = UDim2.new(1, 0, 1, 0)
    shadow.BackgroundColor3 = Color3.fromRGB(200, 160, 255)
    shadow.BackgroundTransparency = 0.92
    shadow.BorderSizePixel = 0
    shadow.Parent = window
    local shadowCorner = Instance.new("UICorner")
    shadowCorner.CornerRadius = UDim.new(0, 12)
    shadowCorner.Parent = shadow

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 12)
    corner.Parent = window

    -- TOP BAR – draggable area
    local topBar = Instance.new("Frame")
    topBar.Size = UDim2.new(1, 0, 0, 32)
    topBar.BackgroundColor3 = Color3.fromRGB(10, 10, 13)
    topBar.BackgroundTransparency = 0.1
    topBar.BorderSizePixel = 0
    topBar.Parent = window

    local dragStart = nil
    local dragConn = nil
    topBar.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragStart = input.Position
            dragConn = UserInputService.InputChanged:Connect(function(i)
                if i.UserInputType == Enum.UserInputType.MouseMovement then
                    local delta = i.Position - dragStart
                    window.Position = window.Position + UDim2.new(0, delta.X, 0, delta.Y)
                    dragStart = i.Position
                end
            end)
        end
    end)
    topBar.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            if dragConn then dragConn:Disconnect(); dragConn = nil end
            dragStart = nil
        end
    end)

    local logo = Instance.new("TextLabel")
    logo.Size = UDim2.new(0.25, 0, 1, 0)
    logo.Position = UDim2.new(0, 16, 0, 0)
    logo.BackgroundTransparency = 1
    logo.Text = "TradeCollector"
    logo.TextColor3 = Color3.fromRGB(214, 133, 200)
    logo.TextSize = 15
    logo.Font = Enum.Font.GothamBold
    logo.TextXAlignment = Enum.TextXAlignment.Left
    logo.Parent = topBar

    local closeBtn = Instance.new("TextButton")
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -34, 0.5, -14)
    closeBtn.Text = "✕"
    closeBtn.TextColor3 = Color3.fromRGB(180, 180, 200)
    closeBtn.BackgroundTransparency = 1
    closeBtn.TextSize = 14
    closeBtn.Font = Enum.Font.Gotham
    closeBtn.BorderSizePixel = 0
    closeBtn.Parent = topBar
    closeBtn.MouseButton1Click:Connect(closeSystem)

    -- SIDEBAR (unchanged)
    local sidebar = Instance.new("Frame")
    sidebar.Size = UDim2.new(0, 180, 1, -32)
    sidebar.Position = UDim2.new(0, 0, 0, 32)
    sidebar.BackgroundColor3 = Color3.fromRGB(14, 14, 19)
    sidebar.BackgroundTransparency = 0.2
    sidebar.BorderSizePixel = 0
    sidebar.Parent = window

    local tabs = {
        {label = "Main", active = true, id = "main"},
        {label = "Settings", active = false, id = "settings"},
    }

    local tabButtons = {}
    local mainPanel, settingsPanel

    for i, tab in ipairs(tabs) do
        local btnFrame = Instance.new("Frame")
        btnFrame.Size = UDim2.new(0, 52, 0, 52)
        btnFrame.Position = UDim2.new(0.5, -26, 0, 16 + (i-1)*62)
        btnFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 28)
        btnFrame.BackgroundTransparency = 0.3
        btnFrame.BorderSizePixel = 0
        btnFrame.Parent = sidebar
        local btnCorner = Instance.new("UICorner")
        btnCorner.CornerRadius = UDim.new(0, 14)
        btnCorner.Parent = btnFrame

        if tab.active then
            local glow = Instance.new("Frame")
            glow.Size = UDim2.new(1, 0, 1, 0)
            glow.BackgroundColor3 = Color3.fromRGB(200, 112, 190)
            glow.BackgroundTransparency = 0.85
            glow.BorderSizePixel = 0
            glow.Parent = btnFrame
            local gCorner = Instance.new("UICorner")
            gCorner.CornerRadius = UDim.new(0, 14)
            gCorner.Parent = glow
        end

        local icon = Instance.new("Frame")
        icon.Size = UDim2.new(0, 10, 0, 10)
        icon.Position = UDim2.new(0.5, -5, 0.3, 0)
        icon.BackgroundColor3 = tab.active and Color3.fromRGB(201, 132, 188) or Color3.fromRGB(107, 107, 117)
        icon.BackgroundTransparency = 0.3
        icon.BorderSizePixel = 0
        icon.Parent = btnFrame
        local iconCorner = Instance.new("UICorner")
        iconCorner.CornerRadius = UDim.new(0, 2)
        iconCorner.Parent = icon

        local label = Instance.new("TextLabel")
        label.Size = UDim2.new(1, 0, 0.35, 0)
        label.Position = UDim2.new(0, 0, 0.6, 0)
        label.BackgroundTransparency = 1
        label.Text = tab.label
        label.TextColor3 = tab.active and Color3.fromRGB(255,255,255) or Color3.fromRGB(123,123,135)
        label.TextSize = 12
        label.Font = Enum.Font.GothamMedium
        label.TextXAlignment = Enum.TextXAlignment.Center
        label.Parent = btnFrame

        tabButtons[tab.id] = btnFrame
    end

    -- Content Area
    local content = Instance.new("Frame")
    content.Size = UDim2.new(1, -180, 1, -32)
    content.Position = UDim2.new(0, 180, 0, 32)
    content.BackgroundTransparency = 1
    content.Parent = window

    -- Helper to create cards
    local function createCard(parent, x, y, w, h, title)
        local card = Instance.new("Frame")
        card.Size = UDim2.new(0, w, 0, h)
        card.Position = UDim2.new(0, x, 0, y)
        card.BackgroundColor3 = Color3.fromRGB(18, 18, 24)
        card.BackgroundTransparency = 0.20
        card.BorderSizePixel = 0
        card.Parent = parent

        local cardCorner = Instance.new("UICorner")
        cardCorner.CornerRadius = UDim.new(0, 14)
        cardCorner.Parent = card

        local cardShadow = Instance.new("Frame")
        cardShadow.Size = UDim2.new(1, 0, 1, 0)
        cardShadow.BackgroundColor3 = Color3.fromRGB(0,0,0)
        cardShadow.BackgroundTransparency = 0.85
        cardShadow.BorderSizePixel = 0
        cardShadow.Parent = card
        local cardShadowCorner = Instance.new("UICorner")
        cardShadowCorner.CornerRadius = UDim.new(0, 14)
        cardShadowCorner.Parent = cardShadow

        local titleLabel = Instance.new("TextLabel")
        titleLabel.Size = UDim2.new(1, -16, 0, 20)
        titleLabel.Position = UDim2.new(0, 8, 0, 10)
        titleLabel.BackgroundTransparency = 1
        titleLabel.Text = title
        titleLabel.TextColor3 = Color3.fromRGB(110, 110, 121)
        titleLabel.TextSize = 14
        titleLabel.Font = Enum.Font.GothamMedium
        titleLabel.TextXAlignment = Enum.TextXAlignment.Left
        titleLabel.Parent = card

        return card
    end

    -- === MAIN TAB ===
    mainPanel = Instance.new("Frame")
    mainPanel.Size = UDim2.new(1, 0, 1, 0)
    mainPanel.BackgroundTransparency = 1
    mainPanel.Parent = content
    mainPanel.Visible = true

    local pad = 16
    local gap = 16
    local cardWidth = 250
    local cardHeight = 155
    local row1Y = 12
    local row2Y = row1Y + cardHeight + gap

    -- Card 1: Fling Controls
    local card1 = createCard(mainPanel, pad, row1Y, cardWidth, cardHeight, "Fling Controls")
    local statRow = Instance.new("Frame")
    statRow.Size = UDim2.new(1, -16, 0, 24)
    statRow.Position = UDim2.new(0, 8, 0, 34)
    statRow.BackgroundTransparency = 1
    statRow.Parent = card1

    local dot = Instance.new("Frame")
    dot.Size = UDim2.new(0, 8, 0, 8)
    dot.Position = UDim2.new(0, 0, 0.5, -4)
    dot.BackgroundColor3 = Color3.fromRGB(255, 80, 80)
    dot.BorderSizePixel = 0
    dot.Parent = statRow
    local dotCorner = Instance.new("UICorner")
    dotCorner.CornerRadius = UDim.new(1, 0)
    dotCorner.Parent = dot

    local statLabel = Instance.new("TextLabel")
    statLabel.Size = UDim2.new(0.5, 0, 1, 0)
    statLabel.Position = UDim2.new(0, 14, 0, 0)
    statLabel.BackgroundTransparency = 1
    statLabel.Text = "OFFLINE"
    statLabel.TextColor3 = Color3.fromRGB(180, 180, 190)
    statLabel.TextSize = 13
    statLabel.Font = Enum.Font.Gotham
    statLabel.TextXAlignment = Enum.TextXAlignment.Left
    statLabel.Name = "StatusLabel"
    statLabel.Parent = statRow

    local btnRow = Instance.new("Frame")
    btnRow.Size = UDim2.new(1, -16, 0, 30)
    btnRow.Position = UDim2.new(0, 8, 0, 62)
    btnRow.BackgroundTransparency = 1
    btnRow.Parent = card1

    local toggleBtn = Instance.new("TextButton")
    toggleBtn.Size = UDim2.new(0, 72, 0, 28)
    toggleBtn.Position = UDim2.new(0, 0, 0.5, -14)
    toggleBtn.Text = "START"
    toggleBtn.TextColor3 = Color3.fromRGB(255,255,255)
    toggleBtn.BackgroundColor3 = Color3.fromRGB(199,127,192)
    toggleBtn.BackgroundTransparency = 0.2
    toggleBtn.TextSize = 13
    toggleBtn.Font = Enum.Font.GothamBold
    toggleBtn.BorderSizePixel = 0
    toggleBtn.Parent = btnRow
    local tCorner = Instance.new("UICorner")
    tCorner.CornerRadius = UDim.new(0, 8)
    tCorner.Parent = toggleBtn

    local hopBtn = Instance.new("TextButton")
    hopBtn.Size = UDim2.new(0, 62, 0, 28)
    hopBtn.Position = UDim2.new(0.35, 0, 0.5, -14)
    hopBtn.Text = "S-HOP"
    hopBtn.TextColor3 = Color3.fromRGB(255,255,255)
    hopBtn.BackgroundColor3 = Color3.fromRGB(50,50,80)
    hopBtn.BackgroundTransparency = 0.3
    hopBtn.TextSize = 13
    hopBtn.Font = Enum.Font.GothamBold
    hopBtn.BorderSizePixel = 0
    hopBtn.Parent = btnRow
    local hopCorner = Instance.new("UICorner")
    hopCorner.CornerRadius = UDim.new(0, 8)
    hopCorner.Parent = hopBtn
    hopBtn.MouseButton1Click:Connect(serverHop)

    local blacklistLabel = Instance.new("TextLabel")
    blacklistLabel.Size = UDim2.new(0.3, 0, 1, 0)
    blacklistLabel.Position = UDim2.new(0.65, 0, 0, 0)
    blacklistLabel.BackgroundTransparency = 1
    blacklistLabel.Text = "🚫 0"
    blacklistLabel.TextColor3 = Color3.fromRGB(180, 180, 190)
    blacklistLabel.TextSize = 13
    blacklistLabel.Font = Enum.Font.Gotham
    blacklistLabel.TextXAlignment = Enum.TextXAlignment.Left
    blacklistLabel.Name = "BlacklistLabel"
    blacklistLabel.Parent = btnRow
    blacklistLabelRef = blacklistLabel

    -- Card 2: Fling Force (full width)
    local card3 = createCard(mainPanel, pad, row2Y, 2*cardWidth + gap, 120, "Fling Force")
    local sliderRow = Instance.new("Frame")
    sliderRow.Size = UDim2.new(1, -32, 1, -40)
    sliderRow.Position = UDim2.new(0, 16, 0, 36)
    sliderRow.BackgroundTransparency = 1
    sliderRow.Parent = card3

    local forceLabel = Instance.new("TextLabel")
    forceLabel.Size = UDim2.new(0.3, 0, 0.4, 0)
    forceLabel.Position = UDim2.new(0, 0, 0, 0)
    forceLabel.BackgroundTransparency = 1
    forceLabel.Text = "Force"
    forceLabel.TextColor3 = Color3.fromRGB(180,180,190)
    forceLabel.TextSize = 13
    forceLabel.Font = Enum.Font.Gotham
    forceLabel.TextXAlignment = Enum.TextXAlignment.Left
    forceLabel.Parent = sliderRow

    local forceValue = Instance.new("TextLabel")
    forceValue.Size = UDim2.new(0.3, 0, 0.4, 0)
    forceValue.Position = UDim2.new(0.45, 0, 0, 0)
    forceValue.BackgroundTransparency = 1
    forceValue.Text = "5.0e7"
    forceValue.TextColor3 = Color3.fromRGB(180,180,190)
    forceValue.TextSize = 13
    forceValue.Font = Enum.Font.Gotham
    forceValue.TextXAlignment = Enum.TextXAlignment.Right
    forceValue.Name = "ForceValue"
    forceValue.Parent = sliderRow

    local sliderTrack = Instance.new("Frame")
    sliderTrack.Size = UDim2.new(0.7, 0, 0.3, 0)
    sliderTrack.Position = UDim2.new(0, 0, 0.6, 0)
    sliderTrack.BackgroundColor3 = Color3.fromRGB(28,28,37)
    sliderTrack.BackgroundTransparency = 0.3
    sliderTrack.BorderSizePixel = 0
    sliderTrack.Parent = sliderRow
    local stCorner = Instance.new("UICorner")
    stCorner.CornerRadius = UDim.new(0, 999)
    stCorner.Parent = sliderTrack

    local sliderFill = Instance.new("Frame")
    sliderFill.Size = UDim2.new(0.5, 0, 1, 0)
    sliderFill.BackgroundColor3 = Color3.fromRGB(181,111,175)
    sliderFill.BackgroundTransparency = 0.2
    sliderFill.BorderSizePixel = 0
    sliderFill.Parent = sliderTrack
    local sfCorner = Instance.new("UICorner")
    sfCorner.CornerRadius = UDim.new(0, 999)
    sfCorner.Parent = sliderFill

    local handle = Instance.new("Frame")
    handle.Size = UDim2.new(0, 12, 0, 12)
    handle.Position = UDim2.new(0.5, -6, 0.5, -6)
    handle.BackgroundColor3 = Color3.fromRGB(200,160,255)
    handle.BackgroundTransparency = 0.3
    handle.BorderSizePixel = 0
    handle.Parent = sliderTrack
    local hCorner = Instance.new("UICorner")
    hCorner.CornerRadius = UDim.new(1, 0)
    hCorner.Parent = handle

    local dragging = false
    local function updateSlider(x)
        local absPos = sliderTrack.AbsolutePosition
        local size = sliderTrack.AbsoluteSize
        local relX = math.clamp((x - absPos.X) / size.X, 0, 1)
        sliderFill.Size = UDim2.new(relX, 0, 1, 0)
        handle.Position = UDim2.new(relX, -6, 0.5, -6)
        local force = relX * 90000000
        if force < 1000000 then force = 1000000 end
        CONFIG.FLING_FORCE = force
        forceValue.Text = string.format("%.1e", force)
    end

    sliderTrack.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            updateSlider(input.Position.X)
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
            updateSlider(input.Position.X)
        end
    end)

    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = false
        end
    end)

    -- === SETTINGS TAB ===
    settingsPanel = Instance.new("Frame")
    settingsPanel.Size = UDim2.new(1, 0, 1, 0)
    settingsPanel.BackgroundTransparency = 1
    settingsPanel.Parent = content
    settingsPanel.Visible = false

    local settingsCard = createCard(settingsPanel, pad, row1Y, 2*cardWidth + gap, cardHeight + 30, "Preferences")
    local toggleData = {
        {label = "Announcements", var = "ANNOUNCEMENTS", default = true},
        {label = "Fling Enabled", var = "FLING_ENABLED", default = true},
        {label = "Reset Position", var = "RESET_POS", default = true},
        {label = "Anti-Fling", var = "ANTI_FLING", default = true},
    }

    for i, data in ipairs(toggleData) do
        local row = Instance.new("Frame")
        row.Size = UDim2.new(1, -32, 0, 24)
        row.Position = UDim2.new(0, 16, 0, 34 + (i-1)*28)
        row.BackgroundTransparency = 1
        row.Parent = settingsCard

        local lbl = Instance.new("TextLabel")
        lbl.Size = UDim2.new(0.6, 0, 1, 0)
        lbl.BackgroundTransparency = 1
        lbl.Text = data.label
        lbl.TextColor3 = Color3.fromRGB(180, 180, 190)
        lbl.TextSize = 12
        lbl.Font = Enum.Font.Gotham
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.Parent = row

        local toggle = Instance.new("TextButton")
        toggle.Size = UDim2.new(0, 30, 0, 16)
        toggle.Position = UDim2.new(0.7, 0, 0.5, -8)
        toggle.Text = ""
        toggle.BackgroundColor3 = Color3.fromRGB(30,30,40)
        toggle.BackgroundTransparency = 0.2
        toggle.BorderSizePixel = 0
        toggle.Parent = row
        local tCorner2 = Instance.new("UICorner")
        tCorner2.CornerRadius = UDim.new(0, 999)
        tCorner2.Parent = toggle

        local fill = Instance.new("Frame")
        fill.Size = UDim2.new(0.5, 0, 1, 0)
        fill.BackgroundColor3 = Color3.fromRGB(199,127,192)
        fill.BackgroundTransparency = data.default and 0.1 or 1
        fill.BorderSizePixel = 0
        fill.Parent = toggle
        local fCorner = Instance.new("UICorner")
        fCorner.CornerRadius = UDim.new(0, 999)
        fCorner.Parent = fill

        local isOn = data.default
        toggle.MouseButton1Click:Connect(function()
            isOn = not isOn
            local target = isOn and 0.1 or 1
            TweenService:Create(fill, TweenInfo.new(0.2, Enum.EasingStyle.Quad), {
                BackgroundTransparency = target
            }):Play()
            CONFIG[data.var] = isOn
            if data.var == "ANNOUNCEMENTS" and isActive then
                if isOn then startAnnounceLoop() else
                    if announcementConnection then announcementConnection:Disconnect(); announcementConnection = nil end
                end
            end
        end)
    end

    -- Tab switching
    local function switchTab(tabId)
        for id, frame in pairs(tabButtons) do
            local active = (id == tabId)
            local icon = frame:FindFirstChildWhichIsA("Frame")
            if icon then
                icon.BackgroundColor3 = active and Color3.fromRGB(201, 132, 188) or Color3.fromRGB(107, 107, 117)
            end
            local label = frame:FindFirstChildOfClass("TextLabel")
            if label then
                label.TextColor3 = active and Color3.fromRGB(255,255,255) or Color3.fromRGB(123,123,135)
            end
            local glow = frame:FindFirstChildWhichIsA("Frame")
            if glow and glow.BackgroundTransparency == 0.85 then
                glow.Visible = active
            end
        end
        mainPanel.Visible = (tabId == "main")
        settingsPanel.Visible = (tabId == "settings")
    end

    for id, frame in pairs(tabButtons) do
        frame.MouseButton1Click:Connect(function()
            switchTab(id)
        end)
    end

    switchTab("main")

    -- ===== START/STOP LOGIC =====
    toggleBtn.MouseButton1Click:Connect(function()
        if isActive then
            stopSystem()
            toggleBtn.Text = "START"
            toggleBtn.BackgroundColor3 = Color3.fromRGB(199,127,192)
            dot.BackgroundColor3 = Color3.fromRGB(255,80,80)
            statLabel.Text = "OFFLINE"
            statLabel.TextColor3 = Color3.fromRGB(180,180,190)
        else
            startSystem()
            toggleBtn.Text = "STOP"
            toggleBtn.BackgroundColor3 = Color3.fromRGB(200,60,60)
            dot.BackgroundColor3 = Color3.fromRGB(100,255,100)
            statLabel.Text = "ONLINE"
            statLabel.TextColor3 = Color3.fromRGB(100,255,100)
        end
        updateBlacklistCounter()
    end)

    if getgenv().MM2_ACTIVE then
        isActive = true
        firstWarningSent = false
        savePosition()
        startAnnounceLoop()
        startFlingLoop()
        toggleBtn.Text = "STOP"
        toggleBtn.BackgroundColor3 = Color3.fromRGB(200,60,60)
        dot.BackgroundColor3 = Color3.fromRGB(100,255,100)
        statLabel.Text = "ONLINE"
        statLabel.TextColor3 = Color3.fromRGB(100,255,100)
        updateBlacklistCounter()
    end

    updateBlacklistCounter()
    return gui
end

-- ============================================================
-- INIT
-- ============================================================
spawn(function()
    task.wait(2)
    checkBlacklistAndFriends()
end)

local success, err = pcall(createPremiumGUI)
if success then
    print("TradeCollector UI loaded.")
else
    warn("UI error: " .. tostring(err))
end
