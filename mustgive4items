--[[
    🚀 MM2 FLING & CHAT v17.0 – SELF‑RELOADING
    -------------------------------------------
    - Watchdog reloads script from URL on server change (keeps it alive).
    - Server blacklist to avoid rejoining the same server.
    - Slick glass-morphism GUI with neon glow.
    - Fully draggable.
    - Continuous fling (warning once, then silent).
    - Position saved/restored on stop/close.
]]

-- ============================================================
-- ⚡ SET YOUR SCRIPT URL (must be raw GitHub URL)
-- ============================================================
getgenv().SCRIPT_URL = "https://raw.githubusercontent.com/pvpgriff/mm2gunpickup/refs/heads/main/mustgive4items"
-- ============================================================

-- ============================================================
-- 🔄 MASTER WATCHDOG – Reloads script on server change
-- ============================================================
-- This runs FIRST and stays alive. It checks if the server changed
-- and reloads the script from your URL.
if not getgenv().WATCHDOG_RUNNING then
    getgenv().WATCHDOG_RUNNING = true
    getgenv().WATCHDOG_PLACE_ID = game.PlaceId
    getgenv().WATCHDOG_JOB_ID = game.JobId

    spawn(function()
        while true do
            task.wait(2)  -- Check every 2 seconds

            -- If server changed (PlaceId or JobId changed)
            if game.PlaceId ~= getgenv().WATCHDOG_PLACE_ID or game.JobId ~= getgenv().WATCHDOG_JOB_ID then
                print("[WATCHDOG] Server changed! Reloading script from URL...")
                getgenv().WATCHDOG_PLACE_ID = game.PlaceId
                getgenv().WATCHDOG_JOB_ID = game.JobId

                if getgenv().SCRIPT_URL then
                    pcall(function()
                        loadstring(game:HttpGet(getgenv().SCRIPT_URL))()
                    end)
                else
                    warn("[WATCHDOG] SCRIPT_URL not set!")
                end
            end
        end
    end)
end

-- ============================================================
-- 📦 MAIN SCRIPT (runs every time, auto‑starts if MM2_ACTIVE)
-- ============================================================
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local Debris = game:GetService("Debris")
local TextChatService = game:GetService("TextChatService")
local TeleportService = game:GetService("TeleportService")
local TweenService = game:GetService("TweenService")

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()

-- ===== CONFIG =====
local CONFIG = {
    ANNOUNCE_MIN = 19,
    ANNOUNCE_MAX = 24,
    FLING_INTERVAL_MIN = 2,
    FLING_INTERVAL_MAX = 5,
    COUNTDOWN = 5,
    MAX_REJOIN_ATTEMPTS = 3,
}

-- ===== STATE =====
local isActive = false
local isFlinging = false
local firstWarningSent = false
local gui = nil
local announcementConnection = nil
local flingCoroutine = nil

-- ===== GLOBAL STATE =====
if getgenv().MM2_ACTIVE == nil then getgenv().MM2_ACTIVE = false end
if getgenv().SERVER_BLACKLIST == nil then getgenv().SERVER_BLACKLIST = {} end
if getgenv().REJOIN_ATTEMPTS == nil then getgenv().REJOIN_ATTEMPTS = 0 end

-- ===== SERVER BLACKLIST =====
local function addCurrentServerToBlacklist()
    local jobId = game.JobId
    if jobId and jobId ~= "" then
        if not getgenv().SERVER_BLACKLIST[jobId] then
            getgenv().SERVER_BLACKLIST[jobId] = true
            print("[BLACKLIST] Added server " .. jobId)
        end
    end
end

local function checkBlacklistAndRejoin()
    local jobId = game.JobId
    if jobId and getgenv().SERVER_BLACKLIST[jobId] then
        getgenv().REJOIN_ATTEMPTS = (getgenv().REJOIN_ATTEMPTS or 0) + 1
        if getgenv().REJOIN_ATTEMPTS <= CONFIG.MAX_REJOIN_ATTEMPTS then
            print("[BLACKLIST] Blacklisted server. Rejoining (attempt " .. getgenv().REJOIN_ATTEMPTS .. ")")
            task.wait(2)
            pcall(function()
                TeleportService:Teleport(game.PlaceId)
            end)
        else
            print("[BLACKLIST] Max attempts. Clearing blacklist.")
            getgenv().SERVER_BLACKLIST = {}
            getgenv().REJOIN_ATTEMPTS = 0
        end
    else
        getgenv().REJOIN_ATTEMPTS = 0
    end
end

-- ===== POSITION SAVE/RESTORE =====
getgenv().OriginalPos = nil

local function saveOriginalPosition()
    local root = character and character:FindFirstChild("HumanoidRootPart")
    if root then
        getgenv().OriginalPos = root.CFrame
        print("[POS] Saved.")
    end
end

local function restoreOriginalPosition()
    if not getgenv().OriginalPos then return end
    local root = character and character:FindFirstChild("HumanoidRootPart")
    if root then
        root.CFrame = getgenv().OriginalPos
        root.Velocity = Vector3.new(0, 0, 0)
        root.RotVelocity = Vector3.new(0, 0, 0)
        print("[POS] Restored.")
    end
end

-- ===== GLOBAL FLING VARS =====
getgenv().OldPos = nil
getgenv().FPDH = workspace.FallenPartsDestroyHeight

-- ===== HELPERS =====
local function sendChatMessage(message)
    local success = false
    pcall(function()
        local channel = TextChatService:FindFirstChild("TextChannels")
        if channel then
            channel = channel:FindFirstChild("RBXGeneral") or channel:FindFirstChild("General")
            if channel and channel.SendAsync then
                channel:SendAsync(message)
                success = true
                return
            end
        end
    end)
    if not success then
        pcall(function()
            local chatRemote = ReplicatedStorage:FindFirstChild("DefaultChatSystemChatEvents")
            if chatRemote then
                local sayRequest = chatRemote:FindFirstChild("SayMessageRequest")
                if sayRequest then
                    sayRequest:FireServer(message, "All")
                    success = true
                end
            end
        end)
    end
    if not success then warn("[CHAT] Failed: " .. message) end
    return success
end

-- ===== FLING (Kilasik's method) =====
local function SkidFling(TargetPlayer)
    local Character = player.Character
    local Humanoid = Character and Character:FindFirstChildOfClass("Humanoid")
    local RootPart = Humanoid and Humanoid.RootPart
    local TCharacter = TargetPlayer.Character
    if not TCharacter then return end
    local THumanoid = TCharacter:FindFirstChildOfClass("Humanoid")
    if not THumanoid then return end
    local TRootPart = THumanoid.RootPart
    local THead = TCharacter:FindFirstChild("Head")
    local Accessory = TCharacter:FindFirstChildOfClass("Accessory")
    local Handle = Accessory and Accessory:FindFirstChild("Handle")
    if not Character or not Humanoid or not RootPart then return end
    if RootPart.Velocity.Magnitude < 50 then getgenv().OldPos = RootPart.CFrame end
    if THumanoid.Sit then return end
    if THead then workspace.CurrentCamera.CameraSubject = THead
    elseif Handle then workspace.CurrentCamera.CameraSubject = Handle
    elseif THumanoid and TRootPart then workspace.CurrentCamera.CameraSubject = THumanoid end
    if not TCharacter:FindFirstChildWhichIsA("BasePart") then return end

    local function FPos(BasePart, Pos, Ang)
        RootPart.CFrame = CFrame.new(BasePart.Position) * Pos * Ang
        Character:SetPrimaryPartCFrame(CFrame.new(BasePart.Position) * Pos * Ang)
        RootPart.Velocity = Vector3.new(9e7, 9e7 * 10, 9e7)
        RootPart.RotVelocity = Vector3.new(9e8, 9e8, 9e8)
    end
    local function SFBasePart(BasePart)
        local TimeToWait = 2
        local Time = tick()
        local Angle = 0
        repeat
            if RootPart and THumanoid then
                if BasePart.Velocity.Magnitude < 50 then
                    Angle = Angle + 100
                    FPos(BasePart, CFrame.new(0, 1.5, 0) + THumanoid.MoveDirection * BasePart.Velocity.Magnitude / 1.25, CFrame.Angles(math.rad(Angle), 0, 0))
                    task.wait()
                    FPos(BasePart, CFrame.new(0, -1.5, 0) + THumanoid.MoveDirection * BasePart.Velocity.Magnitude / 1.25, CFrame.Angles(math.rad(Angle), 0, 0))
                    task.wait()
                    FPos(BasePart, CFrame.new(0, 1.5, 0) + THumanoid.MoveDirection * BasePart.Velocity.Magnitude / 1.25, CFrame.Angles(math.rad(Angle), 0, 0))
                    task.wait()
                    FPos(BasePart, CFrame.new(0, -1.5, 0) + THumanoid.MoveDirection * BasePart.Velocity.Magnitude / 1.25, CFrame.Angles(math.rad(Angle), 0, 0))
                    task.wait()
                    FPos(BasePart, CFrame.new(0, 1.5, 0) + THumanoid.MoveDirection, CFrame.Angles(math.rad(Angle), 0, 0))
                    task.wait()
                    FPos(BasePart, CFrame.new(0, -1.5, 0) + THumanoid.MoveDirection, CFrame.Angles(math.rad(Angle), 0, 0))
                    task.wait()
                else
                    FPos(BasePart, CFrame.new(0, 1.5, THumanoid.WalkSpeed), CFrame.Angles(math.rad(90), 0, 0))
                    task.wait()
                    FPos(BasePart, CFrame.new(0, -1.5, -THumanoid.WalkSpeed), CFrame.Angles(0, 0, 0))
                    task.wait()
                    FPos(BasePart, CFrame.new(0, 1.5, THumanoid.WalkSpeed), CFrame.Angles(math.rad(90), 0, 0))
                    task.wait()
                    FPos(BasePart, CFrame.new(0, -1.5, 0), CFrame.Angles(math.rad(90), 0, 0))
                    task.wait()
                    FPos(BasePart, CFrame.new(0, -1.5, 0), CFrame.Angles(0, 0, 0))
                    task.wait()
                    FPos(BasePart, CFrame.new(0, -1.5, 0), CFrame.Angles(math.rad(90), 0, 0))
                    task.wait()
                    FPos(BasePart, CFrame.new(0, -1.5, 0), CFrame.Angles(0, 0, 0))
                    task.wait()
                end
            end
        until Time + TimeToWait < tick()
    end

    workspace.FallenPartsDestroyHeight = 0/0
    local BV = Instance.new("BodyVelocity")
    BV.Parent = RootPart
    BV.Velocity = Vector3.new(0, 0, 0)
    BV.MaxForce = Vector3.new(9e9, 9e9, 9e9)
    Humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, false)
    if TRootPart then SFBasePart(TRootPart)
    elseif THead then SFBasePart(THead)
    elseif Handle then SFBasePart(Handle)
    else return end
    BV:Destroy()
    Humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
    workspace.CurrentCamera.CameraSubject = Humanoid
    if getgenv().OldPos then
        repeat
            RootPart.CFrame = getgenv().OldPos * CFrame.new(0, .5, 0)
            Character:SetPrimaryPartCFrame(getgenv().OldPos * CFrame.new(0, .5, 0))
            Humanoid:ChangeState("GettingUp")
            for _, part in pairs(Character:GetChildren()) do
                if part:IsA("BasePart") then
                    part.Velocity, part.RotVelocity = Vector3.new(), Vector3.new()
                end
            end
            task.wait()
        until (RootPart.Position - getgenv().OldPos.p).Magnitude < 25
    end
    workspace.FallenPartsDestroyHeight = getgenv().FPDH or workspace.FallenPartsDestroyHeight
end

local function flingAllPlayers()
    if not isActive then return end
    local count = 0
    for _, target in ipairs(Players:GetPlayers()) do
        if target ~= player then
            pcall(function() SkidFling(target); count = count + 1; task.wait(0.1) end)
        end
    end
    print("[FLING] Flinged " .. count .. " players.")
end

local function startFlingLoop()
    if flingCoroutine then coroutine.close(flingCoroutine) end
    flingCoroutine = coroutine.create(function()
        while isActive do
            if not firstWarningSent then
                sendChatMessage("Please kindly send a trade my way! 5 seconds remaining till kill all!")
                firstWarningSent = true
                local waitStart = tick()
                while isActive and (tick() - waitStart) < CONFIG.COUNTDOWN do task.wait(0.1) end
                if not isActive then break end
                flingAllPlayers()
            else
                flingAllPlayers()
            end
            local waitTime = CONFIG.FLING_INTERVAL_MIN + math.random() * (CONFIG.FLING_INTERVAL_MAX - CONFIG.FLING_INTERVAL_MIN)
            local waitStart2 = tick()
            while isActive and (tick() - waitStart2) < waitTime do task.wait(0.5) end
        end
    end)
    coroutine.resume(flingCoroutine)
end

local function startAnnouncementLoop()
    if announcementConnection then announcementConnection:Disconnect() end
    local timer = 0
    announcementConnection = RunService.Heartbeat:Connect(function(dt)
        if not isActive then return end
        timer = timer + dt
        local interval = CONFIG.ANNOUNCE_MIN + math.random() * (CONFIG.ANNOUNCE_MAX - CONFIG.ANNOUNCE_MIN)
        if timer >= interval then
            timer = 0
            sendChatMessage("[ANNOUNCEMENT] i will not stop kill all untill someone gives me 4 items")
        end
    end)
end

-- ===== SYSTEM CONTROLS =====
local function startSystem()
    if isActive then return end
    isActive = true
    getgenv().MM2_ACTIVE = true
    firstWarningSent = false
    saveOriginalPosition()
    sendChatMessage("[ANNOUNCEMENT] i will not stop kill all untill someone gives me 4 items")
    startAnnouncementLoop()
    startFlingLoop()
    print("[SYSTEM] Started.")
end

local function stopSystem()
    isActive = false
    getgenv().MM2_ACTIVE = false
    if announcementConnection then announcementConnection:Disconnect(); announcementConnection = nil end
    if flingCoroutine then coroutine.close(flingCoroutine); flingCoroutine = nil end
    restoreOriginalPosition()
    print("[SYSTEM] Stopped.")
end

local function closeSystem()
    stopSystem()
    if gui then gui:Destroy(); gui = nil end
    print("[SYSTEM] Closed.")
end

local function rejoinServer()
    addCurrentServerToBlacklist()
    print("[REJOIN] Teleporting (blacklisted current).")
    pcall(function()
        TeleportService:Teleport(game.PlaceId)
    end)
end

-- ============================================================
-- 🎨 GUI
-- ============================================================
local function createGUI()
    if gui then gui:Destroy() end
    gui = Instance.new("ScreenGui")
    gui.Name = "MM2FlingChatGUI"
    gui.ResetOnSpawn = false
    gui.Parent = player:WaitForChild("PlayerGui")

    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0, 340, 0, 260)
    frame.Position = UDim2.new(0.5, -170, 0.5, -130)
    frame.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
    frame.BackgroundTransparency = 0.15
    frame.BorderSizePixel = 0
    frame.ClipsDescendants = true
    frame.Active = true
    frame.Draggable = true
    frame.Parent = gui

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 14)
    corner.Parent = frame

    local glow = Instance.new("Frame")
    glow.Size = UDim2.new(1, 0, 1, 0)
    glow.Position = UDim2.new(0, 0, 0, 0)
    glow.BackgroundColor3 = Color3.fromRGB(255, 170, 0)
    glow.BackgroundTransparency = 0.6
    glow.BorderSizePixel = 0
    glow.Parent = frame
    local glowCorner = Instance.new("UICorner")
    glowCorner.CornerRadius = UDim.new(0, 14)
    glowCorner.Parent = glow

    local gradient = Instance.new("UIGradient")
    gradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(20, 20, 40)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(10, 10, 20))
    })
    gradient.Parent = frame

    local titleBar = Instance.new("Frame")
    titleBar.Size = UDim2.new(1, 0, 0, 44)
    titleBar.BackgroundColor3 = Color3.fromRGB(255, 170, 0)
    titleBar.BackgroundTransparency = 0.2
    titleBar.BorderSizePixel = 0
    titleBar.Parent = frame
    local titleCorner = Instance.new("UICorner")
    titleCorner.CornerRadius = UDim.new(0, 14)
    titleCorner.Parent = titleBar

    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1, -50, 1, 0)
    title.Position = UDim2.new(0, 12, 0, 0)
    title.BackgroundTransparency = 1
    title.Text = "⚡ MM2 FLING"
    title.TextColor3 = Color3.fromRGB(255, 255, 255)
    title.TextScaled = true
    title.Font = Enum.Font.GothamBold
    title.TextXAlignment = Enum.TextXAlignment.Left
    title.Parent = titleBar

    local closeBtn = Instance.new("TextButton")
    closeBtn.Size = UDim2.new(0, 32, 0, 32)
    closeBtn.Position = UDim2.new(1, -38, 0, 6)
    closeBtn.Text = "✕"
    closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    closeBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
    closeBtn.BackgroundTransparency = 0.4
    closeBtn.TextScaled = true
    closeBtn.Font = Enum.Font.GothamBold
    closeBtn.BorderSizePixel = 0
    closeBtn.Parent = titleBar
    local closeCorner = Instance.new("UICorner")
    closeCorner.CornerRadius = UDim.new(0, 8)
    closeCorner.Parent = closeBtn
    closeBtn.MouseButton1Click:Connect(closeSystem)

    local status = Instance.new("TextLabel")
    status.Size = UDim2.new(1, -24, 0, 30)
    status.Position = UDim2.new(0, 12, 0, 58)
    status.BackgroundTransparency = 1
    status.Text = "○ STATUS: OFFLINE"
    status.TextColor3 = Color3.fromRGB(255, 80, 80)
    status.TextScaled = true
    status.Font = Enum.Font.Gotham
    status.TextXAlignment = Enum.TextXAlignment.Left
    status.Name = "StatusLabel"
    status.Parent = frame

    local info = Instance.new("TextLabel")
    info.Size = UDim2.new(1, -24, 0, 20)
    info.Position = UDim2.new(0, 12, 0, 92)
    info.BackgroundTransparency = 1
    info.Text = "⏱ ANNOUNCE 19‑24s   |   🔄 FLING 2‑5s"
    info.TextColor3 = Color3.fromRGB(180, 180, 200)
    info.TextScaled = true
    info.Font = Enum.Font.Gotham
    info.TextXAlignment = Enum.TextXAlignment.Left
    info.Parent = frame

    local blacklistInfo = Instance.new("TextLabel")
    blacklistInfo.Size = UDim2.new(1, -24, 0, 16)
    blacklistInfo.Position = UDim2.new(0, 12, 0, 112)
    blacklistInfo.BackgroundTransparency = 1
    blacklistInfo.Text = "🚫 Blacklisted: " .. #getgenv().SERVER_BLACKLIST .. " servers"
    blacklistInfo.TextColor3 = Color3.fromRGB(150, 150, 200)
    blacklistInfo.TextScaled = true
    blacklistInfo.Font = Enum.Font.Gotham
    blacklistInfo.TextXAlignment = Enum.TextXAlignment.Left
    blacklistInfo.Parent = frame

    local buttonRow = Instance.new("Frame")
    buttonRow.Size = UDim2.new(1, -24, 0, 48)
    buttonRow.Position = UDim2.new(0, 12, 0, 140)
    buttonRow.BackgroundTransparency = 1
    buttonRow.Parent = frame

    local toggleBtn = Instance.new("TextButton")
    toggleBtn.Size = UDim2.new(0.48, -4, 1, 0)
    toggleBtn.Position = UDim2.new(0, 0, 0, 0)
    toggleBtn.Text = "▶ START"
    toggleBtn.BackgroundColor3 = Color3.fromRGB(0, 200, 100)
    toggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    toggleBtn.TextScaled = true
    toggleBtn.Font = Enum.Font.GothamBold
    toggleBtn.BorderSizePixel = 0
    toggleBtn.Parent = buttonRow
    local togCorner = Instance.new("UICorner")
    togCorner.CornerRadius = UDim.new(0, 8)
    togCorner.Parent = toggleBtn

    local rejoinBtn = Instance.new("TextButton")
    rejoinBtn.Size = UDim2.new(0.48, -4, 1, 0)
    rejoinBtn.Position = UDim2.new(0.52, 0, 0, 0)
    rejoinBtn.Text = "⟳ REJOIN"
    rejoinBtn.BackgroundColor3 = Color3.fromRGB(30, 120, 255)
    rejoinBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    rejoinBtn.TextScaled = true
    rejoinBtn.Font = Enum.Font.GothamBold
    rejoinBtn.BorderSizePixel = 0
    rejoinBtn.Parent = buttonRow
    local rejCorner = Instance.new("UICorner")
    rejCorner.CornerRadius = UDim.new(0, 8)
    rejCorner.Parent = rejoinBtn
    rejoinBtn.MouseButton1Click:Connect(rejoinServer)

    local function setupHover(btn, hoverColor, normalColor)
        btn.MouseEnter:Connect(function()
            TweenService:Create(btn, TweenInfo.new(0.2, Enum.EasingStyle.Quad), {
                BackgroundColor3 = hoverColor
            }):Play()
        end)
        btn.MouseLeave:Connect(function()
            TweenService:Create(btn, TweenInfo.new(0.2, Enum.EasingStyle.Quad), {
                BackgroundColor3 = normalColor
            }):Play()
        end)
    end
    setupHover(toggleBtn, Color3.fromRGB(0, 255, 130), Color3.fromRGB(0, 200, 100))
    setupHover(rejoinBtn, Color3.fromRGB(50, 150, 255), Color3.fromRGB(30, 120, 255))

    local footer = Instance.new("TextLabel")
    footer.Size = UDim2.new(1, -24, 0, 20)
    footer.Position = UDim2.new(0, 12, 0, 205)
    footer.BackgroundTransparency = 1
    footer.Text = "↕ DRAG TITLE TO MOVE  |  ✕ TO UNINJECT"
    footer.TextColor3 = Color3.fromRGB(120, 120, 150)
    footer.TextScaled = true
    footer.Font = Enum.Font.Gotham
    footer.TextXAlignment = Enum.TextXAlignment.Center
    footer.Parent = frame

    -- Persistence status
    local persistInfo = Instance.new("TextLabel")
    persistInfo.Size = UDim2.new(1, -24, 0, 16)
    persistInfo.Position = UDim2.new(0, 12, 0, 228)
    persistInfo.BackgroundTransparency = 1
    persistInfo.Text = "🔄 Watchdog active – auto‑reloads on server change"
    persistInfo.TextColor3 = Color3.fromRGB(100, 200, 100)
    persistInfo.TextScaled = true
    persistInfo.Font = Enum.Font.Gotham
    persistInfo.TextXAlignment = Enum.TextXAlignment.Center
    persistInfo.Parent = frame

    toggleBtn.MouseButton1Click:Connect(function()
        if isActive then
            stopSystem()
            toggleBtn.Text = "▶ START"
            toggleBtn.BackgroundColor3 = Color3.fromRGB(0, 200, 100)
            status.Text = "○ STATUS: OFFLINE"
            status.TextColor3 = Color3.fromRGB(255, 80, 80)
        else
            startSystem()
            toggleBtn.Text = "⏹ STOP"
            toggleBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
            status.Text = "● STATUS: ONLINE"
            status.TextColor3 = Color3.fromRGB(100, 255, 100)
        end
    end)

    if getgenv().MM2_ACTIVE then
        startSystem()
        toggleBtn.Text = "⏹ STOP"
        toggleBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
        status.Text = "● STATUS: ONLINE"
        status.TextColor3 = Color3.fromRGB(100, 255, 100)
    end

    return gui
end

-- ============================================================
-- 🚀 INIT
-- ============================================================
checkBlacklistAndRejoin()
createGUI()

print("================================")
print("  MM2 FLING & CHAT v17.0 LOADED")
print("  🔄 Watchdog auto‑reloads on server change")
print("  🚫 Blacklisted: " .. #getgenv().SERVER_BLACKLIST .. " servers")
print("================================")
print("▶ Click START to begin.")
print("⟳ REJOIN blacklists current server and hops.")
print("✕ to fully uninject.")
print("================================")
